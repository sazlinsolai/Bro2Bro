

				//<![CDATA[
				function clearThis(obj, defText) {
					//alert(obj.value);
					if (obj.value == defText) obj.value = '';
				}

				function restoreThis(obj, defText) {
					if (obj.value == '') obj.value = defText;
				}
				//]]>